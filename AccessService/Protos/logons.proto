syntax = "proto3";

option csharp_namespace = "AccessService";

service Logon {
  rpc Login (LoginRequest) returns (LoginReply);
}

message LoginRequest {
  string name = 1;
  string password = 2;
}

message LoginReply {
  bool success = 1;
  string uid = 2;
  string role = 3;
}
